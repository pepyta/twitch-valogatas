import { Card, CardContent, Typography } from "@mui/material";

const Contact = () => {
    return (
        <Card>
            <CardContent>
                <Typography variant={"h5"}>
                    Kapcsolat
                </Typography>
            </CardContent>
        </Card>
    );
};

export default Contact;